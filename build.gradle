plugins {
	id 'java-library'
	id 'maven-publish'
	id 'com.zyxist.chainsaw' version '0.3.1'
}

javaModule.name = 'browngu.logging'

group = 'brownshome'
version = 'alpha'

sourceSets {
	main.java.srcDirs = ['src/java']
}

task sourcesJar(type: Jar) {
	from sourceSets.main.allJava
	classifier = 'sources'
}

publishing {
	publications {
		mavenJava(MavenPublication) {
			artifactId = 'logging'
			from components.java
			artifact sourcesJar
		}
	}
}